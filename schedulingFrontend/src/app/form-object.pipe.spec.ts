import { FormObjectPipe } from './form-object.pipe';

describe('FormObjectPipe', () => {
  it('create an instance', () => {
    const pipe = new FormObjectPipe();
    expect(pipe).toBeTruthy();
  });
});
